Realizar el siguiente ejercicio:

1. Crear una base de datos PostgreSQL en Amazon RDS, gestionada por Terraform, y configurar un rol IAM que permita a una aplicación conectarse (por ejemplo, una instancia EC2).

Extra:

Y si queremos que la instancia levante con el cliente PostgreSQL instalado?
   
